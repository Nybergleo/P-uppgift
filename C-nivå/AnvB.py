class user(self,name,mail,socialsecurity,password)
    def __init__(self, name,mail,socialsecurity,password,status):  # Skapar klassens object
        self.name = name
        self.author = mail
        self.status = socialsecurity
        self.password = password
        self.status = status

    def check_auth(self):
        if self.status = "A"
            return True
        else:
            return False


    check accsess

    adding users

    login

    penalty

    removing user